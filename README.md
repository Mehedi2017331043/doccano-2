# doccano-2
This is a simple text annotation tool, using this tool user can annotate word/sub word with label color and will add suggestions.

collect all the static files using:
```  
    python manage.py collcetstatic
```